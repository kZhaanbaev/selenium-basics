package day13.instructor;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

public class _02_Tables {
    public static void main(String[] args) {
        System.setProperty("webdriver.chrome.driver", "/Users/kuba/Desktop/Selenium/libs/chromedriver");
        WebDriver driver = new ChromeDriver();
        driver.manage().window().maximize();

        //Print out all emails

        //Print out all first names

        //Print out all first names with emails

        //Print out all first names with emails but as groups

        //Print out value of First names based on if gender is Male (in Tables page)

        //Class Task 1
    }
}
